# Basic experiment configuration (keep unchanged)
policy_name: cappro
task_name: place_fan
task_config: demo_clean
ckpt_setting: cappro_place_fan_clean_phase_v1_7500
seed: 0
instruction_type: unseen

# Add Parameters You Need
train_config_name: pi05_aloha_robotwin_cappro_place_fan_clean_lora
model_name: cappro_place_fan_clean_phase_v1
checkpoint_id: 7500
pi0_step: 50
action_chunk_size: 20

# Inference uses support video to produce Z_cap; caption text remains train-only.
use_support_context: true
# If true, keep the CapPro checkpoint architecture but pass a null
# support video (black frames with support_image_mask=false) at inference.
mask_support_video: false
random_support: true
support_seed: 0
# The ego views in this bank match the support videos used during training.
support_bank_root: /data/RoboTwin/data/support_data/support_bank
support_id: human_demo_000
support_view: ego
num_support_frames: 8
